#include "main.h"
/**
 * print_alphabet - function prints alphabets in lower case
 *
 * Return: returns (0)
 */
void print_alphabet(void)
{
	char first_alphabet;
	for ( c = 'a'; c <= 'z'; c++)
	{
		_putchar(c)
	}

	_putchar('\n');

	return (0);
}
