#include "main.h"
/**
 * mul - multiplies 2 integers
 * @a: first operand to mutiply
 * @b: second operand to multiply with first
 *
 * Return: returns integer ul of two operands
 */
int mul(int a, int b)
{
	return (a * b);
}
